{
  "name": "optimizador-y-destrabar-windows",
  "version": "1.0.0",
  "description": "Sitio estatico para Optimizador y destrabar Windows.",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "collectCoverageFrom": [
      "js/**/*.js"
    ],
    "coverageDirectory": "coverage"
  }
}
